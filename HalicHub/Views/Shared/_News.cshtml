@model News

<div class="properties pb-20">
    <div class="properties__card">
        <div class="properties__img overlay1">
            <a style="display: block" asp-controller="home" asp-action="NewsDetails" asp-route-url="@Model.Url"><img src="@Model.img" alt=""></a>
            @*<div class="icon">
                    @foreach (var item in Model.newsHCategories)
                    {
                <h3 style="font-size: 1em; padding: 5px; background: #07233d; color: white; border-radius: 8px">@item.NCategory.Name</h3>
                    }
                </div>*@
        </div>
    </div>
    <div class="properties__caption">
        <h3 style="font-weight: 700"><a asp-controller="home" asp-action="NewsDetails" asp-route-url="@Model.Url">@Model.Title</a></h3>
        <p> @Model.Description</p>
    </div>
</div>